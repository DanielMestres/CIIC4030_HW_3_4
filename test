# Custom server communication language

# Initialize a remote server
start-remote()

# Initialize a local server
start-local()

# Sets remote port
set-remote(0210)

# Sets local port
set-local(0200)

# Find remote server with given port to be able to communicate, local runs this
get-remote(0210)

# Find local server with given port to be able to communicate, server runs this
get-local(0200)

# Simple Communication from local to remote server and vice-versa
send("Hello")

# Terminate remote or local server
end()
